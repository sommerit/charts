# Copyright Broadcom, Inc. All Rights Reserved.
# SPDX-License-Identifier: APACHE-2.0

# No-op workflow to keep compatible triggers without external dependencies
name: '[Automation] PR review comment trigger (no-op)'
on:
  workflow_run:
    workflows:
      - '[Automation] PR review labeling'
    types:
      - completed
permissions: {}
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "No-op. Upstream reusable workflow replaced."
